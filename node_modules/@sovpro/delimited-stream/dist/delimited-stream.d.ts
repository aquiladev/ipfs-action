/// <reference types="node" />
import { Transform, TransformCallback } from 'stream';
export declare class DelimitedStream extends Transform {
    private buffer;
    private delimiter;
    private queue;
    private incl_delim;
    constructor(delimiter: Buffer | string, incl_delim?: boolean);
    _transform(chunk: Buffer, _encoding_: string, callback: TransformCallback): void;
    _final(callback: (error?: Error | null) => void): void;
    updateQueue(chunk: Buffer): void;
    pushQueue(): void;
}
